from setuptools import setup

__project__ = "spotify like py"
__version__ = "0.0.1"
__description__ = "a Python module to like stuff based on buttons"
__packages__ = ["spotifylikepi"]

setup(
    name = __project__,
    version = __version__,
    description = __description__,
    packages = __packages__,
)